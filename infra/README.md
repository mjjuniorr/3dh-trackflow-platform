# Infraestrutura

Arquivos e notas de deploy da plataforma 3DH TrackFlow.

- `docker/`: suporte a imagens e execucao local.
- `portainer/`: instrucoes e ajustes para Docker Swarm, Portainer, Traefik e rede `PortainerRede`.
